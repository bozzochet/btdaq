#ifdef Q_CTL
  toCTL->Delete();
#endif

#ifdef Q_LOG
  toLOG->Delete();
#endif

#ifdef Q_GUI
  toGUI->Delete();
#endif

#ifdef Q_RUN
  toRUN->Delete();
#endif

#ifdef Q_DSP
  toDSP->Delete();
#endif

